import styles from "./ApplyBtn.module.scss";

const ApplyBtn = () => {
    return (
        <button
            type="submit"
            className={styles.btn}>
            Apply
        </button>
    );
};

export default ApplyBtn;

